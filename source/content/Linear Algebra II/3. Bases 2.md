### Dimension

Let $V$ be a vector space over a field $F$. Suppose $x_1, \dots, x_n$ and $y_1, \dots, y_m$, then $m = n$.

Let $V$ be a vector space over a field $F$. If the vectors $x_1, \dots, x_n$ form a basis of $V$, we say that $V$ is of finite dimension and we call $N$ the dimension of $V$. We write $dim_F(V)$ or just $dim(V)$ for $n$.

> Basically bases are unique to a vector space, so if there are two bases, they are the same.

> The dimension of a vector space is the number of vectors in the basis.

### Key properties of a basis

* It spans and is independent
* Is a minimal spanning set
* Is a maximal independent set
* Every vector has a unique representation

> Minimal spanning set means remove anything and it no longer spans the vector space
> Maximal independent means if you add anything it will no longer be independent

### Two-out-of-three criterion

If you know:
* Linear independence
* Spanning
* n = $dim_F(V)$

Knowing any of the two imply the third is also true;